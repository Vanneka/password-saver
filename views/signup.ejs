<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Page</title>
</head>

<body>
    <form action="/login" method="POST">
        <input type="text" placeholder="Email here" name="email"  value="<%= typeof email != 'undefined' ? email : '' %>">

        <input type="text" placeholder="Username here" name="username" value="<%= typeof email != 'undefined' ? username : '' %>">

        <input type="password" placeholder="Enter your password here" name="password" value="<%= typeof email != 'undefined' ? password : '' %>">

        <input type="password" placeholder="Reenter your password here" name="password2" value="<%= typeof email != 'undefined' ? password2 : '' %>">
        <button type="submit">Signup</button>
    </form>
</body>
<%- include('./partials/footer'); %>
</html>