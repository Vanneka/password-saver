<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Saver | Signup Page</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="login-box">
        <h2>Signup for a secure account</h2>
        <form action="/signup" method="POST">
            <div class="user-box">
                <input type="email" placeholder="Email here" name="email"
                    value="<%= typeof email != 'undefined' ? email : '' %>"></div>

            <div class="user-box">
                <input type="text" placeholder="Username here" name="username"
                    value="<%= typeof email != 'undefined' ? username : '' %>"></div>

            <div class="user-box">
                <input type="password" placeholder="Enter your password here" name="password"
                    value="<%= typeof email != 'undefined' ? password : '' %>"></div>

            <div class="user-box">
                <input type="password" placeholder="Reenter your password here" name="password2"
                    value="<%= typeof email != 'undefined' ? password2 : '' %>"></div>
                    <ul>
                        <%- include('./partials/messages'); %>
                    </ul>
                    <div class="user-btn">
                        <button type="submit" id="submit">Signup</button>
                    </div>
                    <p id="accessibility-mssg">Already have an account? <a href="/login">Login here.</a></p>
                    <p id="accessibility-mssg"><a href="/">Home</a></p>
        </form>

    </div>
</body>

</html>